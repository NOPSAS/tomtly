Commit og push alle endringer til GitHub.

1. `git add -A`
2. `git status` — vis hva som committes
3. Lag en beskrivende commit-melding basert på endringene
4. `git push origin main`
5. Rapporter tilbake med antall filer og commit-hash
